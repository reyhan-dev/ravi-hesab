new CustomRange(
  document.querySelector(".range-container"),
  {
    min: 0,
    max: 10,
    steps: 1,
    showSteps: true,
    showValue: false,
  }
);

new CustomRange(
  document.querySelector(".range-container-mobile"),
  {
    min: 0,
    max: 10,
    steps: 1,
    showSteps: true,
    showValue: false,
  }
);

